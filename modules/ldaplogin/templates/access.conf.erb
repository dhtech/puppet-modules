# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten

+ : root : LOCAL

# Used by scripts, e.g. croned updatedb
+ : daemon : LOCAL
+ : nobody : LOCAL
+ : nogroup : LOCAL
+ : openldap : LOCAL

# TODO: Fix that it's only added on machines with rancid installed
+ : rancid : LOCAL

# Panic user
+ : dhtech : ALL

# LDAP groups
<% @logon.each do |group| -%>
+ : <%= group[0] %> : <%= group[1] %>
<% end -%>

- : ALL : ALL

