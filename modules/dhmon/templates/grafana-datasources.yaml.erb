# config file version
apiVersion: 1

# list of datasources to insert/update depending
# what's available in the database
datasources:
- name: prometheus
  type: prometheus
  access: server
  orgId: 1
  url: https://dhmon.event.dreamhack.se/prometheus/ 
  version: 1
  editable: false
  isDefault: true
