# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten

# DHCP deploy interface. Boot servers from this interface.
allow-hotplug ens256
iface ens256 inet static
	address <%= @deploy_gateway %>
	netmask <%= @deploy_prefix %>
	up iptables -t nat -A POSTROUTING -s <%= @deploy_network %>/24 -j MASQUERADE

iface ens256 inet6 static
  address fc00::1/64
	up ip6tables -t nat -A POSTROUTING -s fc00::/64 -j MASQUERADE
