# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten
#

####
## Begin DREAMHACK specific configuration
####

$ModLoad imudp
$ModLoad imtcp

$UDPServerRun 514
$InputTCPServerRun 514

$template split,"/var/log/dh/%fromhost%/%syslogseverity-text%.log"
$template aggr,"/var/log/dh/%fromhost%/all.log"

*.* -?split;RSYSLOG_SyslogProtocol23Format

# ircbot
*.*     @77.80.231.166:50007

# rancid-trigger
*.*     @localhost:1514

# Do not put messages from the firewall into the aggregated file
:source, startswith, "d-fw" ~

*.* -?aggr

# Do not put remote messages into the local log files in /var/log
:source, !isequal, "localhost" ~
