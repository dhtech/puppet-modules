# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten

<% @peers.each do |peer| -%>
<link name="<%= peer['fqdn'] %>"
      ipaddr="<%= peer['fqdn'] %>"
      port="9999"
      allowmask="<%= peer['allowmask'] %>"
      timeout="60"
      ssl="gnutls"
      bind=""
      statshidden="no"
      hidden="no"
      sendpass="<%= @secret['peersecret'] %>"
      recvpass="<%= @secret['peersecret'] %>">
<autoconnect period="300" server="<%= peer['fqdn'] %>">
<% end -%>

<link
      name="rocketchat.tech.dreamhack.se"
      ipaddr="rocketchat.tech.dreamhack.se"
      port="9999"
      allowmask="77.80.254.0/23"
      timeout="5m"
      ssl="gnutls"
      bind=""
      statshidden="no"
      hidden="no"
      sendpass="<%= @rocketchatsecret['key'] %>"
      recvpass="<%= @rocketchatsecret['key'] %>">
