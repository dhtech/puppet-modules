[Unit]
Description=Observer
Documentation=https://github.com/dhtech/observer

[Service]
Restart=always
User=root
EnvironmentFile=/etc/default/observer
ExecStart=/opt/observer/observer-arm64 $ARGS
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
