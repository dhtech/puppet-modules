# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten
#
# Any specific extensions should be dropped in /etc/profile.d

if [ -d /etc/profile.d ]; then
  for i in /etc/profile.d/*.sh; do
    if [ -r $i ]; then
      . $i
    fi
  done
  unset i
fi
