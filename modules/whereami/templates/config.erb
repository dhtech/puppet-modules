# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten

WHEREAMI_DB_HOST = '<%= @secret_db_whereami['hostname'] %>'
WHEREAMI_DB_USER = '<%= @secret_db_whereami['username'] %>'
WHEREAMI_DB_NAME = '<%= @secret_db_whereami['username'] %>_<%= @current_event %>'
WHEREAMI_DB_PASS = '<%= @secret_db_whereami['password'] %>'

DHCP_DB_HOST = '<%= @secret_db_dhcpinfo['hostname'] %>'
DHCP_DB_USER = '<%= @secret_db_dhcpinfo['username'] %>'
DHCP_DB_NAME = '<%= @secret_db_dhcpinfo['username'] %>_<%= @current_event %>'
DHCP_DB_PASS = '<%= @secret_db_dhcpinfo['password'] %>'

SECRET_KEY='\x81\xc1\xe6\xaf\xb4\xb1\xe8\xcbk\xe1\xc7:*\xec\xd8>i\x17\xafD#-\x0b)'

SNMP_DIST_USER = '<%= @secret_dist_snmpv3['user'] %>'
SNMP_DIST_PASSWORD = '<%= @secret_dist_snmpv3['auth'] %>'
SNMP_DIST_PRIV_PASSWORD = '<%= @secret_dist_snmpv3['priv'] %>'
SNMP_DIST_SECURITY_LEVEL = 'authPriv'
SNMP_DIST_AUTH_PROTO = 'SHA'
SNMP_DIST_PRIV_PROTO = 'AES128'

SNMP2_ACCESS_COMMUNITY = '<%= @secret_access_snmpv2['ro'] %>'

## 2950 doesn't supprt snmp v3 contexts
## The following section can be ignored for now
#SNMP_ACCESS_USER = 'access'
#SNMP_ACCESS_PASSWORD = ''
#SNMP_ACCESS_PRIV_PASSWORD = ''
#SNMP_ACCESS_SECURITY_LEVEL = 'authPriv'
#SNMP_ACCESS_AUTH_PROTO = 'SHA'
#SNMP_ACCESS_PRIV_PROTO = 'DES'

# The API needs to return the IP address of the client, and needs CORS to
# be enabled (HTTP header Access-Control-Allow-Origin: *)
# It also needs a valid SSL certificate, and dual stack.
IPV6_CHECK_API_ENDPOINT = 'https://icanhazip.com'
