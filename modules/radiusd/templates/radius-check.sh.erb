#!/bin/bash
# AUTOGENERATED BY PUPPET
# All manual changes will be overwritten

HEALTHY=0

cat << _EOF_ | radclient -x localhost auth test &> /dev/null
User-Name = "<%= @radtest_secret['username'] %>"
User-Password = "<%= @radtest_secret['password'] %>"
NAS-Port = 1
Message-Authenticator = 0x00
_EOF_

if [[ "$?" == "0" ]]; then
  HEALTHY=1
fi

echo "radius_healthy ${HEALTHY}" > /var/tmp/export/radius.prom
